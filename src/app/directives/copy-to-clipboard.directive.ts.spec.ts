import { CopyToClipboardDirectiveTs } from './copy-to-clipboard.directive.ts';

describe('CopyToClipboardDirectiveTs', () => {
  it('should create an instance', () => {
    const directive = new CopyToClipboardDirectiveTs();
    expect(directive).toBeTruthy();
  });
});
