import { HighlightDirective } from './highlight.directive.ts';

describe('HighlightDirectiveTs', () => {
  it('should create an instance', () => {
    const directive = new HighlightDirective();
    expect(directive).toBeTruthy();
  });
});
